services:
  - type: web
    name: finderai
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      npm install
    startCommand: node server.js
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: NODE_VERSION
        value: 18
